[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fastchunking"
version = "0.0.3"
description = "Fast chunking library (vendored, patched for modern setuptools)"
license = {text = "Apache Software License"}
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["fastchunking"]
